$(document).ready(function(){

       $("#project-title").typed({
          strings:["TEACHER MIS"],
           typeSpeed:0,
           backSpeed: 0,
           loop:true,
           smartBackspace: true,
       });

   });